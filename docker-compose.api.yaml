                        
services:
  stock_api:
    build: 
      context: ./api
      dockerfile: Dockerfile   
    container_name: stock_api
    image: stock_api:latest
    
    ports:
      - "8000:8000"

    env_file:
      - .env
